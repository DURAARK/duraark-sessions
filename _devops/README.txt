# Info

Scripts in this directory are utilities for building and deploying the service as docker container. In future versions the scripts will be replaced by a combination of 'nscale' and 'generator-puppetmaster'.
